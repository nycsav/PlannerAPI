# Cloud Functions Environment Variables
# Copy this file to .env and fill in your actual keys
# IMPORTANT: .env should NEVER be committed to git
#
# These variables are used by Firebase Cloud Functions for server-side operations:
# - Daily Intelligence card generation
# - Chat intelligence endpoints
# - LinkedIn publishing workflow integration

# ============================================================================
# PERPLEXITY API (Server-side)
# ============================================================================
# Used by: functions/src/generateDiscoverCards.ts, chat-intel.ts, chat-simple.ts
# Get from: https://www.perplexity.ai/settings/api
#
# Steps:
# 1. Go to https://www.perplexity.ai/settings/api
# 2. Click "Create" to generate a new API key
# 3. Copy the full key (starting with "pplx-")
# 4. Paste it below

PPLX_API_KEY=your_perplexity_api_key_here
PPLX_MODEL_FAST=sonar

# ============================================================================
# ANTHROPIC CLAUDE API (Server-side)
# ============================================================================
# Used by: functions/src/generateDiscoverCards.ts
# Get from: https://console.anthropic.com/settings/keys
#
# Steps:
# 1. Go to https://console.anthropic.com/settings/keys
# 2. Click "Create Key" to generate a new API key
# 3. Copy the full key (starting with "sk-ant-")
# 4. Paste it below

ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ============================================================================
# LLM API (Server-side) - CopilotKit
# ============================================================================
# Used by: functions/src/copilot-runtime.ts
#
# OPTION A: OpenRouter (RECOMMENDED - cheaper, more models)
# Get from: https://openrouter.ai/keys
#
# Steps:
# 1. Go to https://openrouter.ai/keys
# 2. Click "Create Key"
# 3. Copy the key (starting with "sk-or-")
# 4. Paste it below
#
# Pricing: GPT-4o-mini via OpenRouter = $0.15/$0.60 per 1M tokens

OPENROUTER_API_KEY=your_openrouter_api_key_here

# OPTION B: OpenAI Direct (if you prefer)
# Get from: https://platform.openai.com/api-keys
# Only used if OPENROUTER_API_KEY is not set

OPENAI_API_KEY=your_openai_api_key_here

# ============================================================================
# N8N INTEGRATION (Optional)
# ============================================================================
# Used by: functions/src/storeN8nCards.ts
# Get from: Your n8n instance → Account Settings → API Keys
#
# This is optional and only needed if you're using n8n for LinkedIn publishing

N8N_API_KEY=your_n8n_api_key_here
N8N_WEBHOOK_URL=your_n8n_webhook_url_here

# ============================================================================
# FIREBASE ADMIN SDK
# ============================================================================
# NOTE: Firebase Admin SDK credentials are NOT kept in .env
# Instead, they're automatically provided by Firebase Cloud Functions runtime
# when deployed to Google Cloud Platform
#
# For local emulation:
# - Run: firebase emulators:start
# - The emulator provides automatic credentials

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
# After rotating API keys:
#
# 1. SET KEYS IN FIREBASE CONFIG:
#    firebase functions:config:set \
#      pplx.api_key="YOUR_NEW_PERPLEXITY_KEY" \
#      anthropic.api_key="YOUR_NEW_ANTHROPIC_KEY"
#
# 2. DEPLOY FUNCTIONS:
#    cd functions && npm run deploy
#
# 3. VERIFY DEPLOYMENT:
#    firebase functions:log --only generateDiscoverCards
#
# 4. TEST MANUALLY:
#    gcloud functions call generateDiscoverCards \
#      --gen2 \
#      --region us-central1 \
#      --project plannerapi-prod
